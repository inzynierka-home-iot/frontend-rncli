import { FC } from 'react';
import { AdditionalControlsProps } from '../AdditionalControls';

export const DistanceDetectorControls: FC<AdditionalControlsProps> = ({
  deviceType,
  botHash,
  botId,
}) => {
  return null;
};
