import { FC } from 'react';
import { AdditionalControlsProps } from '../AdditionalControls';

export const LightDetectorControls: FC<AdditionalControlsProps> = ({
  deviceType,
  botHash,
  botId,
}) => {
  return null;
};
