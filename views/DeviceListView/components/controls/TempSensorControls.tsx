import { FC } from 'react';
import { AdditionalControlsProps } from '../AdditionalControls';

export const TempSensorControls: FC<AdditionalControlsProps> = ({
  deviceType,
  botHash,
  botId,
}) => {
  return null;
};
